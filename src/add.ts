/**
 * Function to calculate sum of all postitive numbers passed as string separated by deliminator
 * @param {String} number 
 * @returns {Number} 
 */

export const add = function(number: string): number {
  let result = 0;
  // TODO: String parsing, validation, exception handling and calculate final result
  return result;
};